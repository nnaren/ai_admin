# Sample gateways. Edit freely; the panel picks up changes within ~250ms.
- id: g-running
  name: Sample Gateway
  port: 9000
  startCommand: "/bin/sh -c 'exec sleep 300'"
  stopCommand: "/bin/sh -c 'kill -TERM $PID'"
  healthUrl: "http://127.0.0.1:9000/health"
